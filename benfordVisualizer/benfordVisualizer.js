const { baseMarkupTemplate, chartMarkupTemplate } = require('./markupTemplate');

class BenfordVisualizer {
  constructor() {
    this.baseMarkupTemplate = baseMarkupTemplate;
    this.chartMarkupTemplate = chartMarkupTemplate;
  }

  createChartHTML(benfordObj) {
    
  }
}

module.exports = BenfordVisualizer;