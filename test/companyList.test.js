const CompanyList = require('../dataCSV/companyList');
jest.mock('../companyList/companyList');

describe('Company List', () => {
  
  beforeEach(() => {
    CompanyList.mockClear();
  })

  it('')

})